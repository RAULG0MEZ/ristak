# ================================================
# PLANTILLA DE CONFIGURACIÓN - RISTAK PRO
# ================================================
# Copia este archivo a .env.local y completa con valores reales
# NUNCA pongas credenciales reales en este archivo
# ================================================

# === SERVIDOR DE PRODUCCIÓN ===
# IP o dominio del servidor de producción
DEPLOY_HOST=your-server-ip-or-domain

# Usuario SSH para deployment (recomendado: crear usuario 'deploy', no usar root)
DEPLOY_USER=deploy-user

# Contraseña SSH (mejor usar SSH keys en lugar de contraseña)
DEPLOY_PASSWORD=your-secure-password

# === BASE DE DATOS ===
# URL completa de conexión a PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# === CUENTAS Y API KEYS ===
# Obtener desde tu dashboard o proveedor
ACCOUNT_ID=acc_your-account-id
DEFAULT_SUBACCOUNT_ID=suba_your-subaccount-id
VITE_ACCOUNT_ID=acc_your-account-id
VITE_DEFAULT_SUBACCOUNT_ID=suba_your-subaccount-id

# === CONFIGURACIÓN DE APLICACIÓN ===
NODE_ENV=production
PORT=3002
API_PORT=3002
VITE_API_URL=https://your-domain.com/api

# === DOMINIOS ===
# Dominio principal de la aplicación
APP_DOMAIN=app.your-domain.com

# Dominio para webhooks
WEBHOOK_DOMAIN=send.your-domain.com

# Dominio para tracking
TRACKING_DOMAIN=track.your-domain.com
ALLOW_CUSTOM_DOMAINS=true

# === SEGURIDAD ===
# Dejar vacío para generar automáticamente, o proporcionar valores seguros
JWT_SECRET=
SESSION_SECRET=

# === EMAIL Y NOTIFICACIONES ===
# Email para certificados SSL y notificaciones
EMAIL_SSL=admin@your-domain.com

# === OPCIONALES ===
# Configuración adicional según necesidades
# REDIS_URL=redis://localhost:6379
# SENTRY_DSN=
# ANALYTICS_ID=

# ================================================
# INSTRUCCIONES:
# 1. Copiar a .env.local: cp .env.example .env.local
# 2. Editar .env.local con valores reales
# 3. Asegurar permisos: chmod 600 .env.local
# 4. NUNCA subir .env.local al repositorio
# ================================================